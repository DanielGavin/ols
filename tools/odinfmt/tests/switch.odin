package odinfmt_test

main :: proc() {
	foos := []string{
		"foo", "bar"}

	for i in 0..<len(foos)   {
		switch foo := foo[i]; {
		case "foo": 
		}
	}

	for i in 0..<len(foos)   {
		switch foo := foo[i]; 
		foo {
		case "foo": 
		}
	}
}
